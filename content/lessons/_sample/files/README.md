# Sample lesson files

These files are bundled with the dev-only `_sample` lesson so the
"Load lesson files" action (#41) has something to copy when running
the e2e suite or `pnpm dev`. Real lessons (#42 onwards) ship their
own starter files.
