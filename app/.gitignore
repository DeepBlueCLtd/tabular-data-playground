# Build / dev artefacts for app/ (Vite + TypeScript + React 18 scaffold)
node_modules/
dist/
.vite/
*.log
*.tsbuildinfo

# Playwright (#35) — screenshots are regenerated on every run; rely on
# the playwright-report artefact uploaded by CI for diagnostics.
playwright-report/
test-results/
e2e/screenshots/
