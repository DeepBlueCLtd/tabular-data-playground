# Tasks: Editor Split Panes (#9)

- [X] T001 Add `secondaryTabId` state + Split / Close split
  buttons in `editor-area.tsx`.
- [X] T002 When `secondaryTabId !== null`, render two
  Monaco panes side-by-side via PanelGroup.
- [X] T003 Update `docs/limitations.md`.
- [X] T004 Build / lint / format pass.
- [X] T005 Three commits.
