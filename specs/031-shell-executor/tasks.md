# Tasks: Mini-shell Executor (#24)

- [X] T001 Create `execute.ts` — `executePipeline`, stage
  router, redirect handling.
- [X] T002 Create `shell-runner.tsx` hook —
  `useShellRunner({ vfs, run })` → `{ runLine }`.
- [X] T003 Wire `terminal-panel.tsx` to use the runner.
- [X] T004 Build / lint / format pass.
- [X] T005 Three commits.
